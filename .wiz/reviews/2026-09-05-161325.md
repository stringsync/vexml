# Review 2026-09-05 16:13:25

2 files changed since master; 10 rules matched, 4 blocks to review.

## Findings

### packages/site/render-config.ts

```
   37  🛑 error    uses an em dash between words in a comment    no-em-dashes
  161  🛑 error    uses an em dash between words in a comment    no-em-dashes
  162  🛑 error    uses an em dash between words in a comment    no-em-dashes
```

🛑 3 problems (3 errors, 0 warnings)

All three are on comment lines this change added. `packages/site/app.tsx` was in the
diff but matched no rule's glob, so no block covered it.

## Fix plan

| agent | file | findings | rules |
| ----- | ---- | -------- | ----- |
| 1 | `packages/site/render-config.ts` | 3 | no-em-dashes |

1 agent, in a worktree of its own. Nothing runs until you say so.
