import { MeasureStyle } from '@/musicxml';

describe(MeasureStyle, () => {
  describe('getStaffNumber', () => {
    it.todo('returns the number of the measure style');

    it.todo('defaults to 1 when number is missing');

    it.todo('defaults to 1 when number is invalid');
  });

  describe('getMultipleRestCount', () => {
    it.todo('returns the multiple rest count');

    it.todo('returns 0 when multiple rest is missing');

    it.todo('returns 0 when multiple rest is invalid');
  });
});
