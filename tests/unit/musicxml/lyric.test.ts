import { Lyric } from '@/musicxml';

describe(Lyric, () => {
  describe('getVerseNumber', () => {
    it.todo('returns the verse number of the lyric', () => {});

    it.todo('defaults to 1 when the verse number is missing', () => {});

    it.todo('defaults to 1 when the verse number is not a valid integer', () => {});
  });

  describe('getComponents', () => {
    it.todo('returns the components of the lyric', () => {});

    it.todo('returns an empty array when the lyric is empty', () => {});
  });
});
