import { Time } from '@/musicxml/time';

describe(Time, () => {
  describe('getStaveNumber', () => {
    it.todo('returns the stave number of the time');
  });

  describe('getBeats', () => {
    it.todo('returns the beats of the time');
  });

  describe('getBeatTypes', () => {
    it.todo('returns the beat types of the time');
  });

  describe('isHidden', () => {
    it.todo('returns true when there is a <senza-misura>');

    it.todo('returns false when there is not a <senza-misura>');
  });

  describe('getSymbol', () => {
    it.todo('returns the symbol of the time');
  });
});
