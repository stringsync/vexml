import { Direction } from '@/musicxml';

describe(Direction, () => {
  describe('getTypes', () => {
    it.todo('returns the direction types of the direction');

    it.todo('defaults to an empty array when direction types are missing');
  });
});
