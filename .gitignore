node_modules
packages/site/dist
.zed
tests/integration/__diffs__/
.DS_Store
.playwright-mcp

# a release under way; the last artifact to land deletes it
RELEASE

# wiz cli scratch (per-run judge accounting)
.wiz/
