import { Measure } from './measure';

describe(Measure, () => {
  describe('getNumber', () => {
    it.todo('returns the measure number');

    it.todo('returns -1 when the measure number is missing');

    it.todo('returns -1 when the measure number is invalid');
  });

  describe('hasWidth', () => {
    it.todo('returns true when the measure width is set');

    it.todo('returns false when the measure width is not set');
  });

  describe('getWidth', () => {
    it.todo('returns the measure width');

    it.todo('returns -1 when the measure width is missing');

    it.todo('returns -1 when the measure width is invalid');
  });

  describe('getStaveCount', () => {
    it.todo('returns the stave count');

    it.todo('returns 1 when the measure stave count is missing');

    it.todo('returns 1 when the measure stave count is invalid');
  });
});
