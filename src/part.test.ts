import { Part } from './part';

describe(Part, () => {
  describe('getId', () => {
    it.todo('returns the ID of the part');
  });

  describe('getMeasures', () => {
    it.todo('returns the measures of the part');
  });
});
