import { Score } from './score';

describe(Score, () => {
  describe('getPartCount', () => {
    it.todo('returns the number of <score-part> elements in the document');
  });

  describe('getPartIds', () => {
    it.todo('returns the part IDs in the document');
  });

  describe('getPartNames', () => {
    it.todo('returns the part names in the document');
  });

  describe('getMeasures', () => {
    it.todo('returns the measures in the document');
  });
});
